# Cupcake Docs Docs!

Documentation is built with [zensical](https://zensical.org/)

## Local Development

1. Install [uv](https://docs.astral.sh/uv/) if you haven't got it already
2. Run `uv init` in the `cupcake/docs` directory (where this readme file is located)
3. Run `uv run zensical serve` to
