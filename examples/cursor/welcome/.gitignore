# Cupcake generated files (created by setup.sh)
.cupcake/
bundle.tar.gz

# Test artifacts (created by setup.sh)
test-events/


# IDE
.idea/
.vscode/

# MacOS
.DS_Store

# Backup files
*.backup.*

# MCP
.mcp/node_modules/
.mcp/package-lock.json

# Cursor MCP config (created by mcp_setup.sh) - but NOT hooks.json
# Note: .cursor/hooks.json must NOT be gitignored or Cursor won't load it
.cursor/mcp.json
.cursor/rules/